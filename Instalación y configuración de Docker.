Comandos utilizados, práctica laboratorio 8.1; Cloud Computing y Contenerización.


Instalación y configuración de Docker.

============================================

https://docs.docker.com/engine/install/rhel/


#Desinstalar versiones anteriores:

sudo dnf remove docker \
                  docker-client \
                  docker-client-latest \
                  docker-common \
                  docker-latest \
                  docker-latest-logrotate \
                  docker-logrotate \
                  docker-engine \
                  podman \
                  runc


#Instalar utilizando repositorio RPM

sudo dnf -y install dnf-plugins-core
sudo dnf config-manager --add-repo https://download.docker.com/linux/rhel/docker-ce.repo

sudo dnf install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

sudo systemctl enable --now docker
sudo systemctl start docker
sudo systemctl status docker

sudo docker run hello-world
docker --version

#Buscar la imagen de Nginx y descargarla:

docker search nginx
docker pull nginx

sudo docker images

sudo mkdir /home/website
cd /home/website/
nano index.html >

<html>
 <head>
  <title>DOCKER x NGINX</title>
 </head>
 <body>
  <H3>Julio Cesar Hernandez Tibrey<br>
  Matrícula: 2025-0702</H3>
 </body>
</html>

sudo docker run -d -p 8888:80 -v /home/website:/usr/share/nginx/html/ nginx

docker ps -a

#En el navegador de nuestra máquina:

127.0.0.1:8888



Extra
================================

# Instalar complementos de dnf para manejar repositorios
sudo dnf -y install dnf-plugins-core

# Agregar el repositorio oficial de Docker para RHEL/Rocky Linux
sudo dnf config-manager --add-repo https://download.docker.com/linux/rhel/docker-ce.repo

# Instalar Docker y sus componentes
sudo dnf install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin -y

# Habilitar e iniciar el servicio Docker
sudo systemctl enable --now docker

# Buscar imágenes disponibles de CUPS en Docker Hub
docker search cups

# Descargar la imagen de CUPS (olbat/cupsd)
docker pull olbat/cupsd

# Crear un directorio local para guardar la configuración de CUPS
sudo mkdir -p /home/cups-config

# Ejecutar el contenedor de CUPS mapeando el puerto 631 y el volumen de configuración
sudo docker run -d \
  --name cups-server \
  -p 631:631 \
  -v /home/cups-config:/etc/cups \
  --restart unless-stopped \
  olbat/cupsd

# Verificar que el contenedor está en ejecución
docker ps

# Abrir en el navegador la interfaz de CUPS
# URL: http://127.0.0.1:631
